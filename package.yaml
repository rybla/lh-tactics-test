name: lh-tactics-test
version: 0.1.0.0
github: "githubuser/lh-tactics-test"
license: BSD3
author: "Author name here"
maintainer: "example@example.com"
copyright: "2022 Author name here"

extra-source-files:
  - README.md
  - ChangeLog.md

# Metadata used when publishing your package
# synopsis:            Short description of your package
# category:            Web

# To avoid duplicated efforts in documentation and dealing with the
# complications of embedding Haddock markup inside cabal files, it is
# common to point users to the README.md file.
description:
  Please see the README on GitHub at
  <https://github.com/githubuser/lh-tactics-test#readme>

dependencies:
  - liquidhaskell == 0.8.10.7
  - liquid-base == 4.15.0.0
  - template-haskell == 2.17.0.0
  - lh-tactics == 0.1.0.7

library:
  source-dirs: src
  # ghc-options:
  #   - -fplugin=LiquidHaskell
  # - -fplugin-opt=LiquidHaskell:--reflection
  # - -fplugin-opt=LiquidHaskell:--ple-local
  # - -ddump-splices
